[package]
name = "argus-rs"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
argus-engine = { path = "../argus-engine" }
argus-winmem = { path = "../argus-winmem" }
evidence-core = { path = "../evidence-core" }
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
