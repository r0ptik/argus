[package]
name = "argus-engine"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
argus-scan = { path = "../argus-scan" }
evidence-core = { path = "../evidence-core" }
iced-x86 = "1"
serde = { version = "1", features = ["derive"] }
